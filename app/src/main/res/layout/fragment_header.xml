<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    style="@style/MainHeader"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:custom="http://schemas.android.com/apk/res-auto"
    >

    <LinearLayout
        style="@style/FragmentHeaderMenuWrapper"
        >

        <ImageView
            android:id="@+id/fragment_header_menu"
            android:src="@drawable/ic_menu"
            style="@style/FragmentHeaderMenuIcon"
            />

        <TextView
            android:text="@string/app_name"
            style="@style/FragmentHeaderAppName"
            />

    </LinearLayout>

    <com.lyft.cityguide.ui.components.Slider
        android:id="@+id/fragment_header_slider"
        style="@style/FragmentHeaderSlider"
        custom:labels="@array/place_categories"
        />

</LinearLayout>